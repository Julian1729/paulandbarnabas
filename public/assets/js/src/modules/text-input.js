/**
 * Text Input
 * Handle label animation on focus
 */
const $ = require('jquery');

// collect all text, password, and email types
var textInputs = $('input[type=text], input[type=password], input[type=email]');

function animateLabel(inputElement){

}
